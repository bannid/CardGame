#ifndef COMMON_H
#define COMMON_H

struct GlobalInfo {
    float windowHeight;
    float windowWidth;
    float openglWidth;
    float openglHeight;
    float aspectRatio;
    bool rescale;
};

#endif 